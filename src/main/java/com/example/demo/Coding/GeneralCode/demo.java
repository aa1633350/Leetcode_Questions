package com.example.demo.Coding.GeneralCode;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.*;

public class demo {

    public static void main(String[] args) throws IOException {
//        Map<String, Integer> count = new HashMap<>();
//        String s ="No matter No No sentences So I No my solutions there and copy some codes";
//        for(String w : s.split(" ")) {
//            count.put(w, count.getOrDefault(w,0)+1);
//        }
//        for(Map.Entry<String, Integer> entry : count.entrySet()) {
//            System.out.println(entry.getKey() + ":" + entry.getValue());
//        }
//
//        Map<Integer,String> startPath = new LinkedHashMap<>();
//        startPath.size();
        List<String> list = Arrays.asList("Apple", "Orange", "Banana");
        String x = "apPle";
        System.out.println(list.stream().anyMatch(x::equalsIgnoreCase));

        List<Character> ans = new LinkedList<>();

    }
}
